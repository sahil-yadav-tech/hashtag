name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build -t sahilyadavteche/amazone:latest .

      - name: Push Docker Image
        run: |
          docker push sahilyadavteche/amazone:latest

      - name: Deploy To EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "Pull latest image..."
            docker pull sahilyadavteche/amazone:latest

            echo "Stop and remove old container..."
            docker stop chat-app || true
            docker rm chat-app || true

            echo "Run new container..."
            docker run -d \
              --restart unless-stopped \
              --name chat-app \
              --env-file /home/ubuntu/amazone/.env \
              -p 9987:9987 \
              sahilyadavteche/amazone:latest

            echo "Remove unused images..."
            docker image prune -af || true

            echo "Container status..."
            docker ps

            echo "Container logs..."
            docker logs chat-app --tail 50