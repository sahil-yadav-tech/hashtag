name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build -t sahilyadavteche/chat-app:main .
          docker tag sahilyadavteche/chat-app:main sahilyadavteche/chat-app:main

      - name: Push Docker Images
        run: |
          docker push sahilyadavteche/chat-app:main
          docker push sahilyadavteche/chat-app:main

      - name: Deploy To EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            docker pull sahilyadavteche/chat-app:main

            docker stop chat-app || true
            docker rm chat-app || true

            docker run -d \
              --restart unless-stopped \
              --name chat-app \
              -p 4000:4000 \
              sahilyadavteche/chat-app:main

            docker ps